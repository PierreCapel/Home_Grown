<form method="post" action="{{ path('plant_type_delete', {'id': plant_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plant_type.id) }}">
    <button class="btn">Delete</button>
</form>
