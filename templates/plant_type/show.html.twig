{% extends 'base.html.twig' %}

{% block title %}PlantType{% endblock %}

{% block body %}
    <h1>PlantType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plant_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plant_type.name }}</td>
            </tr>
            <tr>
                <th>DaysToGrowth</th>
                <td>{{ plant_type.daysToGrowth }}</td>
            </tr>
            <tr>
                <th>DaysToFlowering</th>
                <td>{{ plant_type.daysToFlowering }}</td>
            </tr>
            <tr>
                <th>DaysToHarvest</th>
                <td>{{ plant_type.daysToHarvest }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plant_type_index') }}">back to list</a>

    <a href="{{ path('plant_type_edit', {'id': plant_type.id}) }}">edit</a>

    {{ include('plant_type/_delete_form.html.twig') }}
{% endblock %}
