{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="mt-5 mb-3 main-title text-center">Welcome to Home Grown!</h1>
		<div class="row">
			<div class="col-12">
				<div id="carouselExampleSlidesOnly" class="carousel slide" data-bs-ride="carousel">
					<div class="carousel-inner">
						<div class="carousel-item active">
							<img src="{{ asset('build/images/home1.jpg')}}" class="d-block w-100 rounded" alt="home1">
						</div>
						<div class="carousel-item">
							<img src="{{ asset('build/images/home2.jpg')}}" class="d-block w-100 rounded" alt="home2">
						</div>
						<div class="carousel-item">
							<img src="{{ asset('build/images/home3.jpg')}}" class="d-block w-100 rounded" alt="home3">
						</div>
						<div class="carousel-item">
							<img src="{{ asset('build/images/home4.jpg')}}" class="d-block w-100 rounded" alt="home4">
						</div>
						<div class="carousel-item">
							<img src="{{ asset('build/images/home5.jpg')}}" class="d-block w-100 rounded" alt="home5">
						</div>
					</div>
				</div>
			</div>
		</div>
		<h2 class="mt-3 title text-center">
			Touch one of your active cultures bellow to see your daily report!
		</h2>
		<div class="row">
			{% for culture in cultures %}
				<div class="col-12 text-center d-grid gap-2">
					<a class="btn btn-lg btn-outline-secondary my-3" href="{{ path('culture_show', {'id': culture.id}) }}">{{ culture.name }}</a>
				</div>
			{% else %}
				Oh oh, it seems you have no active culture ! 🤦
				        Let's
				<a href="{{ path('culture_new') }}">
					start your next home culture project!
				</a>
				<a href="{{ path('culture_new') }}">Create new</a>
			{% endfor %}
		</div>
		<div class="row">
			<div class="col-12 my-5 text-center">
				🌞🌞🌞🌞
			</div>
		</div>
	</div>
{% endblock %}
